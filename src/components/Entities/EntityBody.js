import { CardContent, Grid } from '@material-ui/core';

export default function EntityBody() {

    return(
        <CardContent>
            <Grid>
                <div>body</div>
            </Grid>
        </CardContent>
    );
}